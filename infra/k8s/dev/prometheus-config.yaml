apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: dev
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: prometheus
        static_configs:
          - targets:
              - localhost:9090

      - job_name: yolo
        metrics_path: /metrics
        static_configs:
          - targets:
              - yolo:8080

      - job_name: agent
        metrics_path: /metrics
        static_configs:
          - targets:
              - agent:8000