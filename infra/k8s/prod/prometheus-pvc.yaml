apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-pvc
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: ebs-sc

  volumeName: prometheus-prod-pv

  resources:
    requests:
      storage: 5Gi